@page "{id?}"
@model NewModel
@{
ViewData["Title"] = "New Topic";
}

<h1>Make a New Topic@(Model.ParentTopic != null ? $" in {Model.ParentTopic.Title}" : "")</h1>

@if (Model.ParentTopic == null)
{
<p>You're about to make a topic that doesn't have a parent category. Topics without a parent will be hard to find. We recommend searching for a parent topic using the <a asp-page="/Index">topic explorer</a> or the <a asp-page="/Topics/Search">topic search</a>. You can then create your new topic in that category.</p>
}
else
{
<p>You're about to make a new topic that is in the @Model.ParentTopic.Title category. That's great! If you don't think your new topic belongs here, use the <a asp-page="/Index">topic explorer</a> or the <a asp-page="/Topics/Search">topic search</a> to find the right category.</p>
}

<hr>

<form method="POST">
    <div class="form-group">
        <label asp-for="NewTopic.Title"></label>
        <input asp-for="NewTopic.Title" class="form-control" aria-describedby="titleHelp" placeholder="Enter title">
        <small id="titleHelp" class="form-text text-muted">The title will be the first thing others see when looking at your topic. Keep it short and simple.</small>
    </div>
    <div class="form-group">
        <label asp-for="NewTopic.Description"></label>
        <input asp-for="NewTopic.Description" class="form-control" aria-describedby="descriptionHelp" placeholder="Enter description">
        <small id="descriptionHelp" class="form-text text-muted">The description should provide more info about your topic and its sub-topics, if it has any.</small>
    </div>
    <div class="form-group">
        <label asp-for="NewTopic.Description"></label>
        <input asp-for="NewTopic.Description" class="form-control" aria-describedby="descriptionHelp" placeholder="Enter description">
        <small id="descriptionHelp" class="form-text text-muted">The description should provide more info about your topic and its sub-topics, if it has any.</small>
    </div>
    <div class="form-check">
        <input type="checkbox" class="form-check-input" id="exampleCheck1">
        <label class="form-check-label" for="exampleCheck1">Check me out</label>
    </div>
    <button type="submit" class="btn btn-primary">Submit</button>
</form>
